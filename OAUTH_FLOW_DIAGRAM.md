# Google OAuth Flow - Visual Diagram

## 🔄 Complete Authentication Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         GOOGLE OAUTH FLOW                                │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│   USER       │
│  (Browser)   │
└──────┬───────┘
       │
       │ 1. Visits /auth/login or /auth/signup
       │
       ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  FRONTEND: Login/Signup Page                                             │
│  - Shows "Continue with Google" button                                   │
│  - Button has onClick={handleGoogleLogin}                                │
└──────┬───────────────────────────────────────────────────────────────────┘
       │
       │ 2. User clicks "Continue with Google"
       │    window.location.href = OAUTH_ENDPOINTS.googleLogin
       │
       ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  BACKEND: /auth/google                                                    │
│  - Constructs Google OAuth URL                                           │
│  - Redirects to Google                                                   │
└──────┬───────────────────────────────────────────────────────────────────┘
       │
       │ 3. Redirect to Google OAuth consent screen
       │
       ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  GOOGLE: OAuth Consent Screen                                            │
│  - User logs in with Google credentials                                  │
│  - User grants permissions                                               │
└──────┬───────────────────────────────────────────────────────────────────┘
       │
       │ 4. Google redirects with authorization code
       │    /auth/google/callback?code=xxx
       │
       ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  BACKEND: /auth/google/callback                                          │
│  - Receives authorization code                                           │
│  - Exchanges code for access token                                       │
│  - Fetches user profile (email, name)                                    │
│  - Generates JWT token                                                   │
│  - Redirects to frontend                                                 │
└──────┬───────────────────────────────────────────────────────────────────┘
       │
       │ 5. Redirect to frontend callback
       │    {FRONTEND_BASE_URL}/auth/callback?token=<JWT>
       │
       ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  FRONTEND: /auth/callback                                                │
│  - Extracts token from URL query parameter                               │
│  - Stores token in localStorage                                          │
│  - Decodes JWT to get user info                                          │
│  - Stores user email and name                                            │
│  - Shows success message                                                 │
│  - Redirects to home page                                                │
└──────┬───────────────────────────────────────────────────────────────────┘
       │
       │ 6. Redirect to home page
       │    navigate("/")
       │
       ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  FRONTEND: Home Page (/)                                                 │
│  - User is now authenticated                                             │
│  - Token available in localStorage                                       │
│  - User can access protected features                                    │
└──────────────────────────────────────────────────────────────────────────┘
```

## ❌ Error Flow

```
┌──────────────┐
│   USER       │
│  (Browser)   │
└──────┬───────┘
       │
       │ 1-4. Same as success flow
       │
       ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  BACKEND: /auth/google/callback                                          │
│  - Receives authorization code                                           │
│  - ❌ Error occurs (network, config, etc.)                               │
│  - Redirects to frontend with error                                      │
└──────┬───────────────────────────────────────────────────────────────────┘
       │
       │ 5. Redirect to frontend callback with error
       │    {FRONTEND_BASE_URL}/auth/callback?error=<error_type>
       │
       ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  FRONTEND: /auth/callback                                                │
│  - Detects error parameter                                               │
│  - Shows user-friendly error message                                     │
│  - Waits 3 seconds                                                       │
│  - Redirects to login page                                               │
└──────┬───────────────────────────────────────────────────────────────────┘
       │
       │ 6. Redirect to login page
       │    navigate("/auth/login")
       │
       ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  FRONTEND: Login Page                                                    │
│  - User can try again                                                    │
└──────────────────────────────────────────────────────────────────────────┘
```

## 🔑 Data Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         DATA FLOW DIAGRAM                                │
└─────────────────────────────────────────────────────────────────────────┘

GOOGLE                    BACKEND                    FRONTEND
  │                          │                          │
  │  Authorization Code      │                          │
  ├─────────────────────────>│                          │
  │                          │                          │
  │  Access Token            │                          │
  │<─────────────────────────┤                          │
  │                          │                          │
  │  User Profile            │                          │
  │  (email, name)           │                          │
  │<─────────────────────────┤                          │
  │                          │                          │
  │                          │  JWT Token               │
  │                          ├─────────────────────────>│
  │                          │                          │
  │                          │                          │  localStorage:
  │                          │                          │  - auth_token
  │                          │                          │  - user_email
  │                          │                          │  - user_name
```

## 🗂️ Component Interaction

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    FRONTEND COMPONENT STRUCTURE                          │
└─────────────────────────────────────────────────────────────────────────┘

App.tsx
  │
  ├─ Route: /auth/login
  │    └─> Login.tsx
  │         │
  │         ├─ "Continue with Google" button
  │         │   └─> onClick: window.location.href = OAUTH_ENDPOINTS.googleLogin
  │         │
  │         └─ Uses: OAUTH_ENDPOINTS from lib/api.ts
  │
  ├─ Route: /auth/signup
  │    └─> Signup.tsx
  │         │
  │         ├─ "Sign up with Google" button
  │         │   └─> onClick: window.location.href = OAUTH_ENDPOINTS.googleLogin
  │         │
  │         └─ Uses: OAUTH_ENDPOINTS from lib/api.ts
  │
  └─ Route: /auth/callback
       └─> Callback.tsx
            │
            ├─ useSearchParams() to get token/error
            │
            ├─ Success Path:
            │   ├─ Store token in localStorage
            │   ├─ Decode JWT
            │   ├─ Store user info
            │   ├─ Show success message
            │   └─ navigate("/")
            │
            └─ Error Path:
                ├─ Show error message
                └─ navigate("/auth/login")
```

## 📦 localStorage Structure

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      localStorage AFTER LOGIN                            │
└─────────────────────────────────────────────────────────────────────────┘

Key: "auth_token"
Value: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJlbWFpbCI6InVzZXJAZXhhbXBsZS5jb20iLCJuYW1lIjoiSm9obiBEb2UiLCJleHAiOjE3MDk4NTYwMDB9.signature"

Key: "user_email"
Value: "user@example.com"

Key: "user_name"
Value: "John Doe"
```

## 🔐 JWT Token Structure

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         JWT TOKEN STRUCTURE                              │
└─────────────────────────────────────────────────────────────────────────┘

Header:
{
  "alg": "HS256",
  "typ": "JWT"
}

Payload:
{
  "email": "user@example.com",
  "name": "John Doe",
  "exp": 1709856000
}

Signature:
HMACSHA256(
  base64UrlEncode(header) + "." +
  base64UrlEncode(payload),
  JWT_SECRET_KEY
)
```

## 🌐 URL Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           URL TRANSITIONS                                │
└─────────────────────────────────────────────────────────────────────────┘

1. http://localhost:8080/auth/login
   │
   │ User clicks "Continue with Google"
   │
   ▼
2. https://roast-my-startup-api.onrender.com/auth/google
   │
   │ Backend redirects
   │
   ▼
3. https://accounts.google.com/o/oauth2/v2/auth?client_id=...
   │
   │ User logs in with Google
   │
   ▼
4. https://roast-my-startup-api.onrender.com/auth/google/callback?code=xxx
   │
   │ Backend processes and redirects
   │
   ▼
5. http://localhost:8080/auth/callback?token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   │
   │ Frontend processes token
   │
   ▼
6. http://localhost:8080/
   │
   │ User is authenticated
   │
   ✓ Done!
```

## ⏱️ Timing Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         TIMING SEQUENCE                                  │
└─────────────────────────────────────────────────────────────────────────┘

t=0s    User clicks "Continue with Google"
        │
t=0.1s  Redirect to backend /auth/google
        │
t=0.2s  Backend redirects to Google
        │
t=0.3s  Google OAuth consent screen loads
        │
t=5s    User enters credentials and grants permissions
        │
t=5.5s  Google redirects to backend callback
        │
t=5.7s  Backend exchanges code for token
        │
t=6.0s  Backend fetches user profile
        │
t=6.2s  Backend generates JWT
        │
t=6.3s  Backend redirects to frontend callback
        │
t=6.4s  Frontend callback page loads
        │
t=6.5s  Token stored in localStorage
        │
t=6.6s  Success message displayed
        │
t=8.1s  Redirect to home page (after 1.5s delay)
        │
t=8.2s  Home page loads
        │
        ✓ User is authenticated!

Total Time: ~8 seconds (including user interaction)
```

## 🎯 Success Indicators

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      HOW TO VERIFY SUCCESS                               │
└─────────────────────────────────────────────────────────────────────────┘

✅ Browser Console:
   - No errors
   - No warnings

✅ Network Tab:
   - 302 redirect to backend
   - 302 redirect to Google
   - 302 redirect to backend callback
   - 302 redirect to frontend callback
   - 200 on home page

✅ Application Tab → Local Storage:
   - auth_token: "eyJ..."
   - user_email: "user@example.com"
   - user_name: "John Doe"

✅ Visual:
   - Success message appears briefly
   - Redirects to home page
   - No error messages

✅ Functional:
   - User can access protected features (future)
   - Token can be used for API calls (future)
```

## 📊 State Transitions

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      CALLBACK PAGE STATES                                │
└─────────────────────────────────────────────────────────────────────────┘

Initial State: "processing"
│
├─ Token found in URL?
│  │
│  ├─ YES → Try to store token
│  │        │
│  │        ├─ Success → State: "success"
│  │        │            └─> Redirect to home (1.5s)
│  │        │
│  │        └─ Error → State: "error"
│  │                   └─> Redirect to login (3s)
│  │
│  └─ NO → Error found in URL?
│           │
│           ├─ YES → State: "error"
│           │        └─> Redirect to login (3s)
│           │
│           └─ NO → State: "error" (invalid callback)
│                    └─> Redirect to login (3s)
```

This visual diagram provides a complete overview of the Google OAuth flow from start to finish, including all components, data flow, and state transitions.
